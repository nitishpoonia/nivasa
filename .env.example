NEXT_PUBLIC_SANITY_PROJECT_ID=rzigun8l
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01
# Shared secret configured on the Sanity webhook (Studio > API > Webhooks)
# that hits /api/revalidate on publish. Generate with `openssl rand -hex 32`.
SANITY_REVALIDATE_SECRET=
